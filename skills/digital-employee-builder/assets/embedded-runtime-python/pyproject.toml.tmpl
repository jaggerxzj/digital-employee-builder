[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "{{employee-package-name}}-runtime"
version = "0.1.0"
description = "Embedded business runtime for {{employee_name}}"
requires-python = ">=3.11"
dependencies = [
  {{runtime dependency list; preserve compatible source-project versions}}
]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

