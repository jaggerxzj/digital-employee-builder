# HEARTBEAT.md — Periodic Tasks

<!-- Every heartbeat costs tokens; keep under 50 lines; delete this file if there are no periodic needs -->

Check on each heartbeat:

- [ ] Query {{pending items, e.g. "count of tickets awaiting review"}}; report if new ones appear
- [ ] Check {{anomaly metric, e.g. "failed order count"}}; alert if above {{threshold}}
- [ ] {{other routine items}}

When nothing is happening: stay quiet; do not message unprompted.
