# TOOLS.md — Environment Notes

<!-- Record WHERE things are, not HOW to behave (rules live in AGENTS.md); never write secret values — variable names only -->

## MCP Server

- `{{employee_name}}-tools`: {{transport stdio/http}}, entrypoint {{command or URL}}
- Required env vars: `BUSINESS_API_BASE`, `BUSINESS_API_TOKEN` (values injected at runtime)

## Business Systems

- {{system name}} console: {{URL}}
- {{system name}} API docs: {{URL or docs/ path}}

## Other Dependencies

- {{database / message queue / third-party service}}: {{address or reference}}
